import React from 'react'
import {NavLink} from 'react-router-dom'
const Navbar = () => {
  return (
    <>
        {/* <ul>
        <li> <NavLink to={'/'} style={({isActive}) => { return { backgroundColor: isActive ? " yellow " : " " } }}>
                HOME 
             </NavLink> </li>
        <li> <NavLink to={'/regform'} style={({isActive}) => { return { backgroundColor: isActive ? " yellow " : " " } }}>
                Registration  
             </NavLink> </li>
        </ul> */}
    </>
  );
}

export default Navbar ;
